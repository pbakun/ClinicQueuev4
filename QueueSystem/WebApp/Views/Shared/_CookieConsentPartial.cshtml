@using Microsoft.AspNetCore.Http.Features

@{
    var consentFeature = Context.Features.Get<ITrackingConsentFeature>();
    var showBanner = !consentFeature?.CanTrack ?? false;
    var cookieString = consentFeature?.CreateConsentCookie();
}

@if (showBanner)
{
    <div id="cookieConsent" class="alert alert-info alert-dismissible fade show" role="alert" style="padding-right: 6rem;">
        Do prawidłowego działania strony wymagane jest korzystanie z plików cookie (ciasteczek). Dalsze korzystanie ze strony oznacza zgodę na ich użycie.
        Szczegóły nt. wykorzystania ciasteczek znajdziesz w <a asp-area="Patient" asp-controller="Home" asp-action="Privacy">polityce prywatności</a>.
        <button type="button" class="accept-policy close" data-dismiss="alert" aria-label="Close" data-cookie-string="@cookieString">
            <span aria-hidden="true">Akceptuje</span>
        </button>
    </div>
    <script>
        (function () {
            var button = document.querySelector("#cookieConsent button[data-cookie-string]");
            button.addEventListener("click", function (event) {
                document.cookie = button.dataset.cookieString;
            }, false);
        })();
    </script>
}
